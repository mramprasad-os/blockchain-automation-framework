apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: university-university-steward-3-node
  annotations:
    fluxcd.io/automated: "false"
  namespace: university-ns
spec:
  releaseName: university-university-steward-3-node
  chart:
    path: platforms/hyperledger-indy/charts/indy-node
    git: https://github.com/mramprasad-os/blockchain-automation-framework.git
    ref: develop
  values:
    metadata:
      name: university-university-steward-3-node
      namespace: university-ns
    network:
      name: baf
    organization:
      name: university
    genesis:
      add_org: False
    image:
      pullSecret: regcred
      initContainer:
        name: university-university-steward-3-node-init
        repository: alpine:3.9.4
      cli:
        name: university-university-steward-3-node-ledger-txn
        repository: index.docker.io/hyperledgerlabs/indy-ledger-txn:latest
        pullSecret: regcred  
      indyNode:
        name: university-university-steward-3-node
        repository: index.docker.io/hyperledgerlabs/indy-node:1.11.0
    node:
      name: university-steward-3
      ip: 0.0.0.0
      publicIp: 40.75.64.181
      port: 15731
      ambassadorPort: 15731
    client:
      publicIp: 40.75.64.181
      ip: 0.0.0.0
      port: 15732
      ambassadorPort: 15732
    service:
      type: ClusterIP
      ports:
        nodePort: 15731
        nodeTargetPort: 15731
        clientPort: 15732
        clientTargetPort: 15732
    configmap:
      indyConfig: |-
        NETWORK_NAME = 'baf'
        # Enable stdout logging
        enableStdOutLogging = True
        logRotationBackupCount = 10
        # Directory to store ledger.
        LEDGER_DIR = '/var/lib/indy/data'
        # Directory to store logs.
        LOG_DIR = '/var/log/indy'
        # Directory to store keys.
        KEYS_DIR = '/var/lib/indy/keys'
        # Directory to store genesis transactions files.
        GENESIS_DIR = '/var/lib/indy/genesis'
        # Directory to store backups.
        BACKUP_DIR = '/var/lib/indy/backup'
        # Directory to store plugins.
        PLUGINS_DIR = '/var/lib/indy/plugins'
        # Directory to store node info.
        NODE_INFO_DIR = '/var/lib/indy/data'
    ambassador:
      annotations: |-
        ---
        apiVersion: ambassador/v2
        kind: TCPMapping
        name: university-university-steward-3-node-node-mapping
        port: 15731
        service: university-university-steward-3-node.university-ns:15731
        ---
        apiVersion: ambassador/v2
        kind: TCPMapping
        name: university-university-steward-3-node-client-mapping
        port: 15732
        service: university-university-steward-3-node.university-ns:15732
    vault:
      address: http://10.1.3.82:8200
      serviceAccountName: university-university-steward-3-vault-auth
      keyPath: /keys/baf/keys/university-steward-3
      authPath: kubernetes-university-university-steward-3-auth
      nodeId: university-steward-3
      role: ro
    storage:
      data:
        storagesize: 3Gi
        storageClassName: university-azure-storageclass
      keys:
        storagesize: 3Gi
        storageClassName: university-azure-storageclass
